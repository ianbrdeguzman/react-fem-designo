import React from 'react';
import styled from 'styled-components';

const Section = styled.section`
    height: 100vh;
`;

const GraphicDesign = () => {
    return <Section>Graphic Design Page</Section>;
};

export default GraphicDesign;
