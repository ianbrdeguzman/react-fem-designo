import React from 'react';
import styled from 'styled-components';

const Section = styled.section`
    height: 100vh;
`;

const WebDesign = () => {
    return <Section>Web Design Page</Section>;
};

export default WebDesign;
