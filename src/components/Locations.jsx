import React from 'react';
import styled from 'styled-components';

const Section = styled.section`
    height: 100vh;
`;

const Locations = () => {
    return <Section>Locations Page</Section>;
};

export default Locations;
