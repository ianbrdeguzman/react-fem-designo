import React from 'react';
import styled from 'styled-components';

const Section = styled.section`
    height: 100vh;
`;

const AppDesign = () => {
    return <Section>App Design Page</Section>;
};

export default AppDesign;
