import React from 'react';
import styled from 'styled-components';

const Section = styled.section`
    height: 100vh;
`;

const Error = () => {
    return <Section>Error Page</Section>;
};

export default Error;
